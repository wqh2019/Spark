# OpenAI API Configuration
OPENAI_API_KEY=sk-your-api-key-here
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_MODEL=gpt-4

# Session Storage (optional)
# Directory for persistent session storage. Defaults to ~/.spark/sessions if not set.
# SPARK_SESSIONS_DIR=/path/to/sessions
